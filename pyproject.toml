[project]
name = "st-andreas"
version = "1.0.0"
description = "St. Andreas data pipelines and tools"
requires-python = ">=3.11"
dependencies = [
    "openpyxl>=3.0",
    "pymysql>=1.1",
    "python-dotenv>=1.0",
]

[project.scripts]
fetch-members = "fetch_members:main"
